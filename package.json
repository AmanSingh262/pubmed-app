{
  "name": "pubmed-intelligent-filter",
  "version": "1.0.0",
  "description": "Intelligent PubMed Article Filtration System with hierarchical category filtering",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon server/index.js",
    "client": "cd client && npm start",
    "build": "npm install && cd server && npm install && cd ../client && npm install && npm run build",
    "start": "NODE_ENV=production node server/index.js",
    "install-all": "npm install && cd client && npm install",
    "railway:build": "npm install && cd server && npm install && cd ../client && npm install && npm run build"
  },
  "keywords": [
    "pubmed",
    "research",
    "filtering",
    "medical",
    "articles"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.2",
    "cors": "^2.8.5",
    "docx": "^9.5.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "mammoth": "^1.11.0",
    "multer": "^1.4.5-lts.1",
    "node-cache": "^5.1.2",
    "pdf-parse": "^1.1.4",
    "winston": "^3.11.0",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2"
  }
}
