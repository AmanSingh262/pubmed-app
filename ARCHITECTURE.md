# System Architecture & Data Flow

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                           │
│                      (React Frontend)                            │
│  ┌──────────┐  ┌───────────┐  ┌──────────┐  ┌──────────┐      │
│  │  Search  │  │   Study   │  │ Category │  │ Results  │      │
│  │   Bar    │  │ Selector  │  │   Tree   │  │ Display  │      │
│  └──────────┘  └───────────┘  └──────────┘  └──────────┘      │
│         │            │              │              │            │
└─────────┼────────────┼──────────────┼──────────────┼───────────┘
          │            │              │              │
          └────────────┴──────────────┴──────────────┘
                       │
                  [HTTP/REST]
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                      BACKEND SERVER                              │
│                   (Node.js + Express)                            │
│  ┌──────────────────────────────────────────────────────┐      │
│  │               API ENDPOINTS                           │      │
│  │  /api/search  │  /api/categories  │  /api/export    │      │
│  └──────────────────────────────────────────────────────┘      │
│           │                  │                 │                │
│           ▼                  ▼                 ▼                │
│  ┌──────────────┐  ┌────────────────┐  ┌────────────┐         │
│  │   PubMed     │  │     Filter     │  │   Export   │         │
│  │   Service    │  │    Service     │  │  Service   │         │
│  └──────────────┘  └────────────────┘  └────────────┘         │
│         │                  │                                    │
│         │         ┌────────┴────────┐                          │
│         │         │                 │                          │
│         │         ▼                 ▼                          │
│         │  ┌─────────────┐  ┌─────────────┐                   │
│         │  │   Keyword   │  │   Ranking   │                   │
│         │  │   Mapping   │  │  Algorithm  │                   │
│         │  └─────────────┘  └─────────────┘                   │
│         │                                                       │
└─────────┼───────────────────────────────────────────────────────┘
          │
     [HTTPS API]
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    PUBMED E-UTILITIES API                        │
│                    (NCBI Web Services)                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                     │
│  │ ESearch  │  │ ESummary │  │  EFetch  │                     │
│  └──────────┘  └──────────┘  └──────────┘                     │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow Diagram

```
1. USER INITIATES SEARCH
   ┌──────────────────────┐
   │ User enters:         │
   │ - Query: "cefixime"  │
   │ - Type: Animal       │
   │ - Category: PK/Abs   │
   └──────────┬───────────┘
              │
              ▼
2. FRONTEND VALIDATION
   ┌──────────────────────┐
   │ Validate inputs      │
   │ Show loading state   │
   └──────────┬───────────┘
              │
              ▼
3. API REQUEST
   ┌──────────────────────┐
   │ POST /api/search     │
   │ {                    │
   │   query,             │
   │   studyType,         │
   │   categoryPath       │
   │ }                    │
   └──────────┬───────────┘
              │
              ▼
4. BACKEND PROCESSING
   ┌──────────────────────────────────┐
   │ a) Check Cache                   │
   │    └─ Hit? Return cached results │
   │    └─ Miss? Continue...          │
   │                                  │
   │ b) Search PubMed (ESearch)       │
   │    └─ Get article IDs            │
   │                                  │
   │ c) Fetch Details (EFetch)        │
   │    └─ Get full article data      │
   │                                  │
   │ d) Parse XML Response            │
   │    └─ Extract metadata           │
   │                                  │
   │ e) Apply Filters                 │
   │    └─ Match keywords             │
   │    └─ Calculate scores           │
   │                                  │
   │ f) Rank Articles                 │
   │    └─ Sort by relevance          │
   │                                  │
   │ g) Cache Results                 │
   │    └─ Store for 1 hour           │
   └──────────┬───────────────────────┘
              │
              ▼
5. RETURN RESPONSE
   ┌──────────────────────┐
   │ {                    │
   │   totalArticles,     │
   │   filteredArticles,  │
   │   articles: [...],   │
   │   processingTime     │
   │ }                    │
   └──────────┬───────────┘
              │
              ▼
6. FRONTEND DISPLAY
   ┌──────────────────────┐
   │ - Hide loading       │
   │ - Show results       │
   │ - Display stats      │
   │ - Render articles    │
   └──────────────────────┘
```

## 🔄 Filtering Algorithm Flow

```
INPUT: Articles[] + CategoryKeywords
│
├─ FOR EACH Article:
│   │
│   ├─ Initialize score = 0
│   │
│   ├─ CHECK MeSH Terms
│   │   └─ Match found? score += 10
│   │
│   ├─ CHECK Title
│   │   └─ Keyword found? score += 5
│   │
│   ├─ CHECK Abstract
│   │   └─ Keyword found? score += 2
│   │
│   ├─ CHECK Keywords
│   │   └─ Match found? score += 3
│   │
│   ├─ CALCULATE Bonus
│   │   ├─ 3+ match types? score × 1.5
│   │   └─ 2+ match types? score × 1.2
│   │
│   └─ STORE {article, score, matches}
│
├─ FILTER score > 0
│
├─ SORT by score (descending)
│
└─ RETURN top N articles
```

## 💾 Caching Strategy

```
┌─────────────────────────────────────────┐
│         CACHE ARCHITECTURE              │
├─────────────────────────────────────────┤
│                                         │
│  Key Format:                            │
│  search_{query}_{maxResults}            │
│  details_{pmid1,pmid2,...}              │
│                                         │
│  TTL: 3600 seconds (1 hour)             │
│                                         │
│  Cache Hit Flow:                        │
│  ┌──────────┐                           │
│  │ Request  │                           │
│  └────┬─────┘                           │
│       │                                 │
│       ▼                                 │
│  ┌──────────┐    Yes    ┌───────────┐  │
│  │  Cache?  ├──────────►│  Return   │  │
│  └────┬─────┘           └───────────┘  │
│       │ No                              │
│       ▼                                 │
│  ┌──────────┐                           │
│  │   API    │                           │
│  │  Call    │                           │
│  └────┬─────┘                           │
│       │                                 │
│       ▼                                 │
│  ┌──────────┐                           │
│  │  Store   │                           │
│  │  Cache   │                           │
│  └────┬─────┘                           │
│       │                                 │
│       ▼                                 │
│  ┌──────────┐                           │
│  │  Return  │                           │
│  └──────────┘                           │
│                                         │
└─────────────────────────────────────────┘
```

## 🎯 Keyword Matching Logic

```
┌────────────────────────────────────────────────┐
│        KEYWORD MATCHING PROCESS                │
├────────────────────────────────────────────────┤
│                                                │
│  Category Keywords:                            │
│  ┌─────────────────────────────────┐          │
│  │ - keywords: [...]               │          │
│  │ - meshTerms: [...]              │          │
│  │ - textKeywords: [...]           │          │
│  └─────────────────────────────────┘          │
│           │                                    │
│           ▼                                    │
│  ┌─────────────────────────────────┐          │
│  │  Clean & Normalize               │          │
│  │  - Remove [MeSH]/[tiab] tags    │          │
│  │  - Convert to lowercase         │          │
│  │  - Trim whitespace              │          │
│  └─────────────────────────────────┘          │
│           │                                    │
│           ▼                                    │
│  ┌─────────────────────────────────┐          │
│  │  Match Against Article          │          │
│  │                                  │          │
│  │  1. MeSH Terms (Exact match)    │          │
│  │     Score: +10                   │          │
│  │                                  │          │
│  │  2. Title (Contains)             │          │
│  │     Score: +5                    │          │
│  │                                  │          │
│  │  3. Abstract (Contains)          │          │
│  │     Score: +2                    │          │
│  │                                  │          │
│  │  4. Keywords (Contains)          │          │
│  │     Score: +3                    │          │
│  └─────────────────────────────────┘          │
│           │                                    │
│           ▼                                    │
│  ┌─────────────────────────────────┐          │
│  │  Apply Multipliers               │          │
│  │  - Count match types             │          │
│  │  - 4 types: ×1.5 (50% bonus)    │          │
│  │  - 3 types: ×1.5 (50% bonus)    │          │
│  │  - 2 types: ×1.2 (20% bonus)    │          │
│  └─────────────────────────────────┘          │
│           │                                    │
│           ▼                                    │
│  ┌─────────────────────────────────┐          │
│  │  Final Relevance Score           │          │
│  └─────────────────────────────────┘          │
│                                                │
└────────────────────────────────────────────────┘
```

## 📦 Component Hierarchy

```
App
├── ToastContainer
├── Header
├── Container
│   ├── SearchSection
│   │   ├── SearchBar
│   │   └── StudyTypeSelector
│   │
│   └── MainContent
│       ├── Sidebar
│       │   ├── FilterCard
│       │   │   └── CategoryTree
│       │   │
│       │   └── SelectedCategoryInfo
│       │
│       └── ContentArea
│           ├── WelcomeMessage (initial)
│           │
│           ├── LoadingSpinner (loading)
│           │
│           └── Results (after search)
│               ├── ExportOptions
│               └── ResultsDisplay
│                   ├── ResultsHeader
│                   │   ├── Stats
│                   │   └── Meta
│                   │
│                   └── ArticlesList
│                       └── ArticleCard[]
│                           ├── Header
│                           ├── Info
│                           ├── Abstract
│                           ├── Matches
│                           └── MeSH Terms
```

## 🔐 Security Layers

```
┌────────────────────────────────────────┐
│         SECURITY MEASURES              │
├────────────────────────────────────────┤
│                                        │
│  1. Input Layer                        │
│     ├─ Input validation                │
│     ├─ XSS prevention                  │
│     └─ SQL injection protection        │
│                                        │
│  2. API Layer                          │
│     ├─ Rate limiting                   │
│     ├─ CORS configuration              │
│     └─ Request size limits             │
│                                        │
│  3. Data Layer                         │
│     ├─ XML parsing safety              │
│     ├─ Data sanitization               │
│     └─ Error handling                  │
│                                        │
│  4. Authentication (Future)            │
│     ├─ User sessions                   │
│     ├─ API key protection              │
│     └─ Role-based access               │
│                                        │
└────────────────────────────────────────┘
```

## 📈 Performance Optimization

```
┌────────────────────────────────────────┐
│      PERFORMANCE STRATEGIES            │
├────────────────────────────────────────┤
│                                        │
│  Frontend:                             │
│  ├─ Component memoization              │
│  ├─ Lazy loading                       │
│  ├─ Debounced search                   │
│  └─ Optimized re-renders               │
│                                        │
│  Backend:                              │
│  ├─ In-memory caching                  │
│  ├─ Efficient XML parsing              │
│  ├─ Stream processing                  │
│  └─ Connection pooling                 │
│                                        │
│  API:                                  │
│  ├─ Batch requests                     │
│  ├─ Concurrent processing              │
│  ├─ Response compression               │
│  └─ Rate limit optimization            │
│                                        │
└────────────────────────────────────────┘
```

## 🔄 State Management

```
React App State
├── query (string)
├── studyType ('animal' | 'human')
├── categories (object)
├── selectedCategory (object | null)
├── searchResults (object | null)
│   ├── totalArticles
│   ├── filteredArticles
│   ├── articles[]
│   └── processingTime
├── loading (boolean)
└── showResults (boolean)

State Flow:
User Action → setState → Re-render → Update UI
```

This architecture ensures scalability, maintainability, and optimal performance!
